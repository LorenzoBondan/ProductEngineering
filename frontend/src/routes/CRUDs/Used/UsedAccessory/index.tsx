import { Outlet } from "react-router-dom";

const UsedAccessories = () => {
    return (
        <div className="container">
            <Outlet />
        </div>
    );
}

export default UsedAccessories;