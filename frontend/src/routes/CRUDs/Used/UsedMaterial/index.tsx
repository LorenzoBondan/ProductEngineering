import { Outlet } from "react-router-dom";

const UsedMaterials = () => {
    return (
        <div className="container">
            <Outlet />
        </div>
    );
}

export default UsedMaterials;