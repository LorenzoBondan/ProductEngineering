import { Outlet } from "react-router-dom";

const GuideMachines = () => {
    return (
        <div className="container">
            <Outlet />
        </div>
    );
}

export default GuideMachines;