import { Outlet } from "react-router-dom";

const MachineGroups = () => {
    return (
        <div className="container">
            <Outlet />
        </div>
    );
}

export default MachineGroups;